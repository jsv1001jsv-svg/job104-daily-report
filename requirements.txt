# 版本鎖定：避免不同時間 build 出不同環境（可重現建置）

# --- Web / Webhook ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- HTTP client（呼叫 OpenRouter / LINE、抓 104 靜態分類表）---
httpx==0.28.1

# --- 抓 104：Cloudflare 是靠 TLS 指紋辨識機器人，curl_cffi 模仿瀏覽器的
#     TLS/HTTP2 handshake 即可通過，不需要真的開瀏覽器（見 CLAUDE.md 5.1）---
curl_cffi==0.16.0

# --- 設定驗證：啟動時檢查必要環境變數，快速失敗 ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- 資料庫 ---
google-cloud-firestore==2.20.0

# --- LINE Messaging API ---
line-bot-sdk==3.14.2

# --- 本機排程模擬（正式環境由 Modal Cron 取代）---
apscheduler==3.11.0
